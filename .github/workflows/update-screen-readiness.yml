name: Update screen readiness

on:
  push:
    branches:
      - "architecture/**"
    paths:
      - "docs/SCREENS.yaml"
      - "docs/ROUTES.yaml"
      - "docs/ACTIONS.yaml"
      - "docs/screens/**"
      - "scripts/capture_profile_validation.py"
      - "scripts/generate_screen_readiness.py"
      - "scripts/requirements.txt"
      - ".github/workflows/update-screen-readiness.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r scripts/requirements.txt

      - name: Capture profile validation
        run: python scripts/capture_profile_validation.py

      - name: Generate readiness matrix
        run: python scripts/generate_screen_readiness.py

      - name: Commit diagnostic and audit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/PROFILE_VALIDATION_DIAGNOSTIC.txt docs/SCREEN_READINESS.md docs/SCREEN_READINESS.yaml
          if git diff --cached --quiet; then
            echo "Diagnostic and readiness audit are already current."
          else
            git commit -m "Capture Profile validation result"
            git push
          fi
