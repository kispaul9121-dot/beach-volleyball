name: Update screen readiness

on:
  push:
    branches:
      - "architecture/**"
    paths:
      - "docs/SCREENS.yaml"
      - "docs/ROUTES.yaml"
      - "docs/ACTIONS.yaml"
      - "docs/screens/**"
      - "scripts/migrate_contextual_management.py"
      - "scripts/generate_screen_readiness.py"
      - "scripts/requirements.txt"
      - ".github/workflows/update-screen-readiness.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r scripts/requirements.txt

      - name: Apply contextual management migration
        run: python scripts/migrate_contextual_management.py

      - name: Generate readiness matrix
        run: python scripts/generate_screen_readiness.py

      - name: Commit architecture and audit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs scripts/validate_profile_activity.py
          if git diff --cached --quiet; then
            echo "Architecture and readiness audit are already current."
          else
            git commit -m "Move creation and management into Games and Camps"
            git push
          fi
