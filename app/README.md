# Будущий код приложения

Когда начнётся реализация, структура Expo Router должна следовать `docs/ROUTES.yaml`.

Документация хранится централизованно в `docs/`, а подробности каждого экрана — отдельными файлами в `docs/screens/`.
